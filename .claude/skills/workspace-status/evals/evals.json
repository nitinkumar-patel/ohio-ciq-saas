{
  "skill_name": "workspace-status",
  "evals": [
    {
      "id": 1,
      "prompt": "workspace status",
      "expected_output": "The agent invokes the workspace-status backend (python3 <skill-dir>/scripts/workspace_status.py status --root <root>) and surfaces the initiative queue and repository backlog from the JSON output. For this fixture: one active initiative ('Example Initiative') with one queued item that is ready to start — spec/example-feature has no blocking dependencies, so the backend classifies it as ready even though no spec.md exists (missing spec files affect reconciliation, not readiness). It also renders 'Backlog — 2 open item(s)' followed, in source order, by the untyped example-build entry labeled [build] and the typed example-shape entry labeled [shape]. The agent does NOT re-derive readiness, reconciliation, or backlog membership independently — all classification and backlog data comes from the backend JSON.",
      "assertions": [
        "Invokes the backend script rather than independently parsing workspace.toml or recomputing the DAG",
        "Reports the active initiative name from the JSON output ('Example Initiative')",
        "Reports the queued spec as ready to start (no blocking dependencies in the fixture)",
        "Renders a dynamically counted two-item Backlog section from repo_backlog.open",
        "Reports example-build as [build] before example-shape as [shape]"
      ],
      "files": ["evals/files/workspace.toml"],
      "expect": {
        "produces": [],
        "output_contains": ["Example Initiative", "Backlog", "2 open item", "[build]", "example-build", "[shape]", "example-shape"],
        "output_excludes": ["Traceback", "workspace-status error:"]
      }
    },
    {
      "id": 2,
      "prompt": "Run repair-plan on the workspace to see what queue fixes are available",
      "expected_output": "The agent invokes the workspace-status repair-plan subcommand (python3 <skill-dir>/scripts/workspace_status.py repair-plan --root <root>) and reports the plan result. The plan JSON includes workspace_fingerprint, plan_id, automatic_operations, and manual_findings. The agent summarises which operations are automatic and which require manual review. The agent does NOT modify workspace.toml.",
      "assertions": [
        "Invokes repair-plan rather than modifying workspace.toml directly",
        "Reports the workspace_fingerprint from the plan JSON",
        "Distinguishes automatic_operations from manual_findings"
      ],
      "files": ["evals/files/workspace.toml"],
      "expect": {
        "produces": [".workspace-repair-plan.json"],
        "output_contains": ["repair-plan"],
        "output_excludes": ["Traceback", "workspace-status error:"]
      }
    },
    {
      "id": 3,
      "prompt": "workspace status",
      "expected_output": "The agent invokes the workspace-status backend and omits the Backlog section because repo_backlog.open is empty.",
      "assertions": [
        "Uses repo_backlog.open from the backend JSON as the repository backlog contract",
        "Does not render a Backlog section for an empty repository backlog"
      ],
      "files": ["evals/files/empty/workspace.toml"],
      "expect": {
        "produces": [],
        "output_contains": [],
        "output_excludes": ["Backlog —", "Traceback", "workspace-status error:"]
      }
    },
    {
      "id": 4,
      "prompt": "workspace status",
      "expected_output": "The agent invokes the workspace-status backend and renders the target five-field repository backlog in source order, using each path as the identifier and the backend-provided room: [shape] docs/product/intents/example.md, then [build] docs/specs/example-defect/spec.md.",
      "assertions": [
        "Uses target entry paths rather than expecting legacy slugs",
        "Reports the intent entry as [shape] and the defect entry as [build]",
        "Preserves source order"
      ],
      "files": ["evals/files/target/workspace.toml"],
      "expect": {
        "produces": [],
        "output_contains": ["Backlog", "[shape]", "docs/product/intents/example.md", "[build]", "docs/specs/example-defect/spec.md"],
        "output_excludes": ["Traceback", "workspace-status error:"]
      }
    },
    {
      "id": 5,
      "prompt": "Show workspace status, including the tracker refresh authority and availability facts",
      "expected_output": "The agent invokes the workspace-status backend and reports only its structured tracker authority projection: tracker-origin, profile example-service 1.0, compared revision remote-rev-2, accepted revision remote-rev-1, unresolved conflict, and refresh/write-back availability unknown. It does not expose field ownership, the approver identity, raw tracker content, or infer that a processor is installed.",
      "assertions": [
        "Uses the backend authority projection rather than parsing prose or tracker content",
        "Reports compared and accepted revisions plus the unresolved conflict",
        "Reports refresh and write-back availability as unknown until a processor provides explicit facts",
        "Does not expose owned fields or approver identity"
      ],
      "files": [
        "evals/files/refresh/workspace.toml",
        "evals/files/refresh/docs/specs/tracker-backed/spec.md",
        "evals/files/refresh/docs/specs/tracker-backed/plan.md"
      ],
      "expect": {
        "produces": [],
        "output_contains": ["tracker-origin", "example-service", "remote-rev-2", "remote-rev-1", "unknown"],
        "output_excludes": ["Outcome", "Example Approver", "Traceback", "workspace-status error:"]
      }
    },
    {
      "id": 6,
      "prompt": "Show whether this all-shipped initiative can be closed and what I should do next.",
      "expected_output": "The agent uses workspace-status only for a read-only projection of pause, closeout blockers, all-specs-shipped eligibility, cooling-context visibility, and the instruction to invoke close-work. It does not distil content, choose a disposition, confirm authority, compact coordination, remove entries, or delete.",
      "assertions": [
        "Projects eligibility and blockers without choosing policy",
        "Directs the user to close-work for closeout",
        "Performs no write, compaction, or deletion"
      ],
      "files": ["evals/files/workspace.toml"],
      "expect": {
        "produces": [],
        "output_contains": ["close-work"],
        "output_excludes": ["deleted", "compacted", "Traceback", "workspace-status error:"]
      }
    }
  ]
}
