# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
.loop-run/

# Conductor scratch + per-workspace probe scaffolding (agentbundle).
.context/

# cognito-auth-chatbot feature (docs/specs/cognito-auth-chatbot) — Terraform
# state, secrets, and frontend build/env artifacts.
infra/terraform/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
frontend/.env.local
frontend/node_modules/
infra/terraform/build/
